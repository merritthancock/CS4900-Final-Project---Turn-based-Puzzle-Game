import {sleep, currentLevel} from "./Global.js";

//Animates basic movement of an entity
function moveAnimate(entity, fromX, fromZ, toX, toZ) {
    /*let clips = entity.animations;
    let clip = THREE.AnimationClip.findByName( clips, 'move' );
    let action = entity.mixer.clipAction( clip );
    action.play();
    
    //return to idle
    clip = THREE.AnimationClip.findByName( clips, 'idle' );
    action = entity.mixer.clipAction( clip );
    action.play();*/
}

export {moveAnimate};